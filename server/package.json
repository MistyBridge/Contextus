{
  "name": "@contextus/server",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Contextus Web 本地服务：树快照 API + 节点操作 + SSE 实时事件",
  "scripts": {
    "dev": "tsx src/index.ts",
    "start": "tsx src/index.ts",
    "test": "tsx --test test/*.test.ts"
  },
  "dependencies": {
    "@fastify/static": "^8.0.0",
    "fastify": "^5.2.0"
  }
}
